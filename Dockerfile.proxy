FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir fastapi uvicorn sentence-transformers httpx && \
    rm -rf ~/.cache/huggingface
CMD ["python", "backend/scripts/openai_proxy.py"]
